#!/usr/bin/env python3
"""Kestrel operational memory CLI entry point."""

import sys
from pathlib import Path

# Allow running as ./tools/kb without package install
ROOT = Path(__file__).resolve().parent
sys.path.insert(0, str(ROOT))

from kb_lib.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
